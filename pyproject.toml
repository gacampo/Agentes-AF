[project]
name = "agentes-af"
version = "0.1.0"
description = "Sistema multi-agente de análisis de inversión basado en Claude"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.39.0",
    "rich>=13.0.0",
    "pydantic>=2.0.0",
]

[project.scripts]
analizar = "main:main"
